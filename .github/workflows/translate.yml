name: Auto Translate

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**/*.md'

jobs:
  translate:
    # 🌟 核心优化：如果提交代码的人是 GitHub 机器人本身，直接跳过，防止无限循环
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run translation script
        env:
          LLM_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          python translate.py

      - name: Commit and push translated files
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          if ! git diff --cached --quiet; then
            # 🌟 核心优化：这里去掉了 [skip ci]，这样 Cloudflare 每次都会高高兴兴地自动部署您翻译好的网站！
            git commit -m "docs: auto translate markdown files via DeepSeek"
            # 🌟 核心修复：推送前先自动合并云端最新的修改，防止冲突拒绝！
            git pull --rebase origin HEAD:${{ github.ref }}
            git push origin HEAD:${{ github.ref }}
          else
            echo "No changes to commit"
          fi